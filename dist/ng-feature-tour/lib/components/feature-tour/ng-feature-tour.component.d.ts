import { OnInit } from '@angular/core';
import { Step, Setup } from '../../ng-feature-tour.model';
import { FeatureTourService } from '../../ng-feature-tour.service';
import * as i0 from "@angular/core";
export declare class NgFeatureTourComponent implements OnInit {
    private featureTourService;
    steps: Step[];
    enabled: boolean;
    setup: Setup;
    constructor(featureTourService: FeatureTourService);
    private disableStep;
    private disableTour;
    private scrollToTop;
    private emitChangeEvent;
    private getBounds;
    private applyBounds;
    private captureFocus;
    private enableStep;
    ngOnInit(): void;
    onEscapeHandler(event?: KeyboardEvent): void;
    close(step: Step): void;
    isLastStep(stepIndex: number): boolean;
    isFirstStep(stepIndex: number): boolean;
    getAriasIdentifiers(target: string): string;
    previous(currentStep: Step, currentStepIndex: number): void;
    next(currentStep: Step, currentStepIndex: number): void;
    finish(currentStep: Step): void;
    abort(currentStep: Step): void;
    static ɵfac: i0.ɵɵFactoryDef<NgFeatureTourComponent, never>;
    static ɵcmp: i0.ɵɵComponentDefWithMeta<NgFeatureTourComponent, "ng-feature-tour", never, { "steps": "steps"; }, {}, never, never>;
}
